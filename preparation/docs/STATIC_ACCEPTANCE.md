# Batch 0 静态验收记录

## 当前 R6 验收范围

R6 已获用户确认并落地。当前 4 场景、47 图片、50 Sprite、1 个表现组件、0 个玩法脚本；指定画幅和世界静态缩放的证据以 VISUAL_R6.md 为准。1A 已授权但开发暂停，本轮仅同步文档。

仍待 0→1A：三态抓手挂点/Collider、首次平台调用边界与动态 resize；微信真实安全区和设备性能留待真实平台验证。当前抓手只显示短索与爪体，无可见横向导轨，物品间无垫板。

## P3 与 P4 历史记录

以下数字、零脚本表述和局部显示结论只对应当时版本，不覆盖 R6，也不构成当前开工阻塞清单。

修订：2026-09-12-P4。**四场景资源导入与 Web 调试构建通过，12 个初始画幅/场景组合加载通过；这不是 Batch 0 全部功能或 Batch 1 玩法验收。** 用户已完成 Creator 登录；重启后的本轮成功恢复工程。

最小方案仍为四个无脚本原生场景及本地验收页，没有引入 UI 框架、适配器或玩法代码。`review/engine.html` 使用实际构建的 Cocos 3.8.8；外部 QA 按钮只负责加载场景、读取公开布局数据及静态世界缩放，不进入 assets 或发布包。`review/layout.html` 仍是 Canvas 构图参考。

## 已执行

| 项目 | 结果与证据 |
|---|---|
| 素材/场景导入 | 35 PNG 对应 SpriteFrame 由 Creator 导入；4 scene 成功参与构建并运行，41 个 Sprite 引用。引用/哈希检查通过 |
| 原生序列化修正 | 依据引擎 widget.ts 校正底部锚点为 LEFT(8)+BOT(4)=12；旧值 33 为 TOP+RIGHT。原生资源刷新后核对，参考页同步正确标志 |
| 全屏背景 | 背景独立于 SafeArea，城市底部对齐 Canvas。天空源组件带烤入的圆角/边框，已另存内部区域作为背景，Alpha 归一 255，等比 cover；未修改正式原图 |
| 设置按钮白线 | P3 重画高光方案已弃用。P4 从正式 result_btn_share.png 清除文字/图标，保留原生边框与高光；图标遮住的一小段用同图右侧补齐。完整 Sprite 等比缩放；Home / Settings 实际引擎目视通过，详见 BUTTON_REVIEW_P4.json |
| Web 构建 | Creator 构建任务 success；首轮约 21 秒，P4 增量构建约 3 秒。最终文件清单与 SHA 见 review/evidence/BUILD_REPORT.json；日志见 web-build.log |
| 画幅/加载 | 375×667、375×812、600×800 三种 CSS 初始视口 × Home/HUD/Settings/Result，P3 共 12 组成功；背景投影覆盖检查均通过。P4 仅替换同尺寸按钮 PNG，另复核 Home / Settings 的 375×667 显示，未冒称重跑全部 12 组 |
| HUD 边界 | HUD 场景的挂牌、暂停、NEXT、旋转、道具条与文字投影边界未越界；非 HUD 页面面板与按钮进行了目视检查，未将其空 HUD 集合检查误称为所有节点检查 |
| 世界/HUD 隔离 | 600×800 初始视口下，World_1x scale=(0.75,0.75,1)；HUD 投影边界与 1x 完全一致。仅静态缩放，不代表事故镜头动画通过 |
| 源文件保护 | 179 张源 PNG 与前轮清单逐个哈希一致；24 项物理比例保持原值。0 个玩法脚本、0 个物理体 |

## 尚未通过

1. **运行中改尺寸**：旧 QA 页面仅修改已运行 iframe 的 CSS 尺寸时，出现可视尺寸滞后、SafeArea 锚点偏移、HUD 裁切。当前验收页明确按新画幅重新启动，验证的是不同设备的初始尺寸；未修复或通过运行中 resize。该反例必须进入平台生命周期/布局实现验收。
2. **真实安全区**：浏览器没有刘海、微信胶囊；构图页模拟的顶部 48/底部 32 只属于夹具，不能签收微信避让或真机。
3. **抓手与 Collider 校准**：当前 HUD 只展示窄爪组合。三态挂点一致性、导轨滑座与索缆/物体的运行连接、物理尺寸/重心/碰撞体尚未标定。
4. **美术/音频主观签收**：12 项 P2 修复已获用户接受；本次白线/背景按用户反馈修正并记录新哈希，不冒用 P2 旧哈希批准。音频保持 41 个未试听候选。沙发、马桶、哑铃、鲸鱼在 1C 前仍需修复并签收。
5. **环境错误分开记录**：浏览器注入的 Electron 包装脚本报 MutationObserver 错误；重复 Creator 启动实例关闭时出现 user 子进程 EPIPE。后续构建成功，未把环境错误隐去，也未将其等同于游戏资源报错。详情见 RUNTIME_REVIEW.json。

构建成功不证明输入、物理、三星事故、全塔辅助、音频混音、真机性能或发布可用。当前没有金币、等级、关卡、商城、广告或云服务实现。
