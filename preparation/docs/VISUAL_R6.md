# R6 已确认布局落地

用户确认：采用整体审核稿，修改为物品之间不放木板、道具竖向排列，并授权开始修改工程。本批仍是 Batch 0 视觉实现，未授权跳至完整玩法。

## 最小方案与复杂度

复用现有 Sprite 与节点即可完成，不增加 UI 框架、新资产或玩法状态。顶部高度 (22,0)、左侧三星 (43,148)、右侧暂停及 NEXT 使用审核坐标。抓手只显示短索与爪体，横向导轨不出现在画面中。Rig 固定从安全区上沿进入，塔体/地面仍按既有视野适配；世界缩放不改变 HUD。

道具使用正式双槽底座图片旋转为竖向，容器位于左下，宽约158、高308；当前只有对称加号，不包含道具文字。后续道具图标作为独立节点保持正向，不能跟随底座旋转。没有重新绘制原图。

首页展示塔与 HUD 高空示例均取消物品间木板垫层，物品直接接触。这里取消的是装饰隔板，不改变正式木板物体在后续物体池中的产品规则。开局仍只有地面承台与待放物，无预置塔。

## 验收边界

以实际引擎截图验证地面、云层、太空及初始短/长屏布局。高度预览仍是 QA 输入，示例塔无物理；没有把视觉尺寸用作 Collider。音频候选仍未试听批准，Batch 1 未启动。

参考：../design/R6/02-proposal.png 为原审核稿；用户本次两项修改覆盖其中横排双槽与木板示例。验收截图记录最终工程，不能把原稿当作最新运行图。

## 已执行验收

- Creator 3.8.8 最终 Web 构建：2026-09-12 22:18:58 success，最新构建段无 error / Missing class。
- 实际画面检查：375×667 地面/云海、600×800 地面、375×812 太空；首页展示塔已取消垫板。短抓手、顶部高度、左侧三星、右侧 NEXT、竖排双槽均实际显示。
- 世界 0.75 缩放前后，高度牌、暂停、NEXT、旋转按钮、InventoryColumn 的 bounds 完全一致（R6-ZOOM.json）。最后一次补充屏外示例纸箱，缩放时塔底继续延伸，见 R6-space-zoom-final.png。
- TypeScript 5.8.3 `tsc --noEmit --skipLibCheck` 通过；跳过引擎声明库，不把它写成引擎全量类型检查。
- 完整性检查通过：179 个正式源 PNG 哈希未变，24 项物理比例未变；4 场景、47 图片、50 Sprite、1 个表现组件、0 个玩法脚本。

证据在 ../review/evidence/R6-*.png；最终地面画面为 R6-ground-final.png。当前结论仅为已确认布局的工程落地与上述显示检查，真实触摸/物理/微信胶囊/真机性能不在本次验证内。
