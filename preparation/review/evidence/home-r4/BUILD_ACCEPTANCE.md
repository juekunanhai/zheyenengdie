# Home R4：来源、范围与构建验收

用户确认首页采用原试听候选 A《Carefree》，对局保留 B《Happy Boy End Theme》，并将正常对局音乐音量从 0.22 提至 0.28。没有授权微信配置变更或上传发布。

## 最小实现范围

- `game-music.ts` 只修改正常音量 0.22 → 0.28，事故/人声压低音量 0.09 不变。
- `scene-actions.ts` 增加首页单个音乐播放源、首次真实触碰解锁、后台/返回处理、导航与销毁取消，以及两首音乐的必要静态署名。原 Result 行为和设置开关代码保持不变。
- `Home.scene` 只在既有 StackSceneActions 组件新增 `homeMusicClip` 引用。
- 在既有 `assets/batch1/audio` 中新增 `bgm_home.mp3` 与 meta，不创建新素材目录。38 个音频中的原 37 个文件及 meta、95 张图、物理、难度和工程配置保持不变。

现有场景控制器内一个可空的首页 AudioSource 足够。首页退出或后台停止时清空 clip 并销毁对应节点，避免旧异步加载在返回后恢复；不增加播放队列、跨场景音频管理器或新的配置层。

## R3 基线继承

改动前原 R3 `verify_recorded_build` 实际 exit 0，312 个输入和 213 个现有产物与冻结 R3 完全一致。`BEFORE.json` 保存 324 个含 profiles 的输入哈希；源码、场景、meta、配置另存于 `before/`，所有 TypeScript 快照使用 `.ts.txt`，没有可被工程扫描的归档 `.ts` 文件。

旧 R3 的 213 个构建输出和日志保存于 `before/build/web-desktop` / `before/temp`，按原 R3 报告逐文件核对哈希，清单见 `FROZEN_R3_OUTPUTS.json`。新门禁读取冻结的旧源码及仍保持不变的旧素材来核对旧输入，并复用原 R3 音频来源、授权、母带和解码检查。旧 R3 recorder、报告和引擎证据未修改，没有声称重跑旧七项引擎测试。

## 首页音乐来源

A 为 Kevin MacLeod《Carefree》，许可 CC BY 4.0。循环选择原曲 19.92–59.92 秒，64 拍 / 16 小节，40 秒、1,764,000 个采样，原速原调；只做周期边界处理、音量调整和编码。

验证链包含获批 A 试听文件、作者完整 MP3、原来源清单、许可、处理脚本、母带、编码文件和新导入清单。运行文件与候选逐字节一致，961,767 字节。实际解码重新计算采样数量及接缝指标，与清单逐值相符；技术接缝通过不等于主观听感或微信设备播放验收。

## 当前构建

首轮 Creator 实际 exit 36，项目既有 TypeScript `--noEmit --skipLibCheck` 实际 exit 0。Creator 日志构建开始/完成为 2026-09-14 19:42:37–19:42:40。314 个输入、309 个保留原文件、214 个输出已记录于 `BUILD_R1.json`；原始日志、TypeScript 日志和执行来源保存在同目录。

日志保留七条诊断匹配行：资源库计时缺失、build-script 子进程 SIGTERM、旧编辑器窗口 JSON 恢复失败，以及 MachPort / parent died 诊断。未称零诊断，也不声称引擎所有第三方声明经过完整类型检查。

完整 `verify_preparation.py` 会继续原静态素材、几何、来源和场景检查，仅将当前音频数与构建报告切到本次明确修订。新录制器拒绝非成功真实退出码、陈旧源码时间、产物与日志哈希变化，以及范围外修改。

本轮完整门禁实际 exit 0，记录于 `INTEGRITY_R1.json` / `VERIFY_R1_EXECUTION.json`：38 个运行音频合计 2,233,859 字节，原 37 个音频全部保留。`GATE_NEGATIVE_CHECKS.json` 的六项只读内存对抗检查通过，失败退出码、物理或设置哈希变化、获批 B 文件改变及未批准新增声音均被拒绝。

`ENGINE_R1.json` 的六项本轮引擎检查全部通过：A 自然循环、导航切换、设置开关、模拟后台/返回、对局音量及人声压低、三轮快速场景切换。`ENGINE_PENDING_R1.json` 额外记录导航与模拟可见性两项严格补测：在同步调用栈内、尚无 STARTED / playing 的加载阶段取消，同样通过。程序化触发、受控高度与模拟 hide/show 均明确标注，未当作手机操作或自然塔高证据。

`MANUAL_UI_R1.json` 记录实际 375×667 浏览器操作：冷启动保持静默，真实点击首页空白区域后 A 以 0.28 播放；真实齿轮进入设置，五行署名可见且无重叠/裁切，再返回首页；点击开始进入 HUD 后 B 以 0.28 播放。画面在工具输出中人工检查，没有声称保存了本地截图。

最终 QA 脚本快照为 `QA_FINAL.js.txt`。初次六项测试加载过更早的脚本版本，未将最终快照哈希伪称为初次加载脚本的哈希。运行时未因补测改变，当前首轮构建保持有效；没有为重复出记录而重新构建。

微信、真机及操作系统真实后台表现仍未验收；未上传或发布。
