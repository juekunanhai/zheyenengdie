# R13 结算视觉与引擎接入

**当前状态：R13 组件已接入真实 Cocos，本地自验通过，待用户引擎视觉复核。** 用户此前回复“确认”，批准了包含新纪录、技术分、玩家比例和分享区域的完整视觉稿，随后授权组件接入与引擎验收。完整稿审核阶段曾按“暂不接入游戏”执行；该历史范围与检查保留在下文及 `QA.json`，不代表当前仍未接入。

R12 保留为原图参考，R13 完整稿是已批准的视觉版本。实际结算仅接入当前已有真实高度、关闭与重开通路；新纪录、技术分、玩家百分比和分享仍隐藏，未推进 1B。

## 审阅入口

- [R13 引擎运行审阅](../../review/result-r13.html) · [本轮引擎检查与复核记录](engine/REVIEW.md)：当前组件组装与实际按钮通路。
- [完整视觉稿审阅页](index.html)：原图/重绘等宽并排，可切换原尺寸、放大、叠加和深浅底色；完整图中的数值及功能区域仍是视觉示例。
- `full-card-r13.png`：1182×1330 RGBA 的完整重绘视觉稿，包含母版的新纪录、技术分、玩家比例、分享区域。
- `evidence/comparison.png`：原母版、修正前、修正后三联。
- `assets/`：10 组独立组件候选，共23张PNG（包括0—9、小数点、m、一张字形图集，以及保留的无彩纸底板）。

## 当前引擎接入与本地检查

最小方案复用已有 `ResultPresentation.ts` 与 `Result.scene`，原样导入 18 张已确认的 PNG：底板、歪塔、标题、气泡、重试、关闭和 12 个成绩字形。仅这两个已有运行文件发生修改；原有 67 张图片保留，当前工程为 85 PNG、18 MP3、10 个 TypeScript（7 个玩法、3 个表现）及 4 场景。难度、物理、Home、HUD、Settings、场景动作和音频均未改。无需新场景系统、运行时状态层或通用组件框架，不采用更复杂方案。

本地检查已覆盖：

- 首箱真实稳定后的 peak 为 1.0m，结束页显示 1.0m；这与受控数字样例检查分别记录。
- 实际关闭按钮返回 Home；实际重开按钮进入 HUD，releases、placed、peak 均归零。
- 零值、42.7、12345.6 数字，以及 375×667、300×650、450×600 画幅；重试图片按 meta 原比例显示。
- TypeScript 实际退出 0、Creator 实际退出 36，当前构建报告见 [BUILD_REPORT.json](engine/BUILD_REPORT.json)。最终完整性检查退出 0，85 图、18 项 R13 导入、来源比例及保护文件哈希通过。构建和本地自验不代替用户对引擎组装视觉的复核，也不扩大到微信真机或发布验收。

## 完整视觉稿阶段记录

该阶段的最小方案为完整视觉稿、独立图片和母版坐标表；当时没有接入游戏，也没有运行 Cocos 构建。以下生成、修图与完整稿批准记录保留原有历史语境。

## 自查后做出的修正

| 项目 | 本轮处理 |
| --- | --- |
| 构图 | 回到母版短白纸标题、黄色蓝边成绩字、歪塔、红色新纪录、白色数据区、竖向分享卡、宽扁黄色按钮 |
| 错误尖角 | 移除标题纸片多出的气泡尾及数据板上边多出的尖角 |
| 成绩 | 缩小m，再按等宽对照适当加宽42.7m整体，避免单位过早收口 |
| 下半部 | 根据独立审查将数据、分享、重试及卡底位置下调 |
| 关闭 | 放大按钮并调整与面板顶沿关系 |
| 透明图 | 生成源把棋盘格画入RGB，按已获授权的本地处理去除外侧背景；保护白脸、白字和蓝灰物体 |

复审结论：角色类别、物体顺序和整体构图已接近母版，可作为完整视觉稿审核。**并非逐像素一致**：数据区/分享/关闭仍有数像素锚点差异；数据白板右缘更规整；重绘数字与高光存在细节差异。原始卡片只有298×336像素，不能据此声称每个细微反光已得到精确还原。

## 组件与完整稿的关系

完整稿是重绘的整体视觉示例，数值已烤入该示例图。独立组件分别生成或从本轮生成图提取，完整稿获批时仍是后续组装候选；本次从中原样导入 18 张并完成本地引擎自验，用户对组装视觉的复核仍待完成。

`COMPONENT_LAYOUT.json`保存低分辨率母版测量范围、层级与坐标；组件图有透明留边和少量比例差异，组装需检查实际alpha可见范围，不可直接以整张PNG边界拉伸。当前组装的检查与差异另见 [引擎记录](engine/REVIEW.md)。独立底板已补入从本轮生成稿提取的彩纸，处理记录见`CONFETTI_PROCESSING.json`。右侧塔底被原按钮遮挡的部分按同一蓝色物体补全，不能宣称原图可以验证被遮挡像素。

12个成绩字形已接入真实高度显示。完整稿中的2680和92%仍是示例；当前运行页不展示技术分、玩家百分比、新纪录或分享，也未新增相关逻辑。

## 来源与授权

原图：`../result-r12/reference-only/result-card.png`，只供参考/比对。没有从历史UI抠取生产像素。

用户明确允许本地处理素材，也允许所有图片作为内置图像生成工具的参考。实际仅传图片，未传代码或其他文件。

- `MAIN_GENERATION.json` / `LAYOUT_CORRECTION_GENERATION.json`：完整稿、数据板、成绩字形的提示词和来源。
- `COMPONENT_ART.json`：底板与歪塔来源、原图保存副本与alpha处理。
- `FIXED_LABELS.json`：标题、新纪录、气泡、分享的生成与清理记录。
- `LOCAL_PROCESSING.json` / `process_main_art.py`：完整稿、重试、关闭、数据板的本地清理配方。
- `HEIGHT_GLYPHS.json`：字形坐标和清理记录。
- `QA.json`：完整视觉稿阶段的RGBA/哈希、工程范围校验与视觉检查历史记录；当前引擎接入另见 `engine/`。

完整视觉稿阶段曾核对212个工程资源/配置文件，未发现修改或新增，当时未启动或构建Cocos，难度、物理、音频、首页和游戏内结算实现均保持原状。这项历史检查不覆盖随后授权的本次组件接入；当前改动范围及证据以顶部说明和 [引擎记录](engine/REVIEW.md) 为准。
